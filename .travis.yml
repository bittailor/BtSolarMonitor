language: c++
sudo: false
compiler: gcc
os:
  - linux
addons:
  apt:
    packages:
      - ninja-build
before_install:
  - gem update --system
  - gem install rake
install:
  - rake install
script:
  - rake host:test
notifications:
  email:
    on_success: change
    on_failure: change
